# 环境配置模板
# 本地开发:  cp .env.example .env     （读 .env）
# 生产部署:  cp .env.example .env.prod （读 .env.prod，并设 APP_ENV=prod）
#
# .env / .env.prod 已被 .gitignore 排除，不会提交到 git，
# 密码等敏感信息只放这两个文件里。

# 运行环境: local=本地开发 / prod=生产
APP_ENV=local

# ===== 数据库 =====
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=game_platform
